{
  "entities": {
    "User": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "User",
      "type": "object",
      "description": "Represents a user of the NotaKu application.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the User entity."
        },
        "email": {
          "type": "string",
          "description": "User's email address.",
          "format": "email"
        },
        "firstName": {
          "type": "string",
          "description": "User's first name."
        },
        "lastName": {
          "type": "string",
          "description": "User's last name."
        },
        "displayName": {
          "type": "string",
          "description": "User's full display name."
        },
        "role": {
          "type": "string",
          "description": "The role of the user, defaults to 'user'.",
          "enum": [
            "admin",
            "user"
          ]
        },
        "nik": {
          "type": "string",
          "description": "User's National Identification Number (NIK)."
        },
        "phone": {
          "type": "string",
          "description": "User's phone number."
        },
        "registrationStatus": {
          "type": "string",
          "description": "The registration status of the user's account.",
          "enum": [
            "pending",
            "approved"
          ]
        }
      },
      "required": [
        "id",
        "email",
        "role",
        "registrationStatus"
      ]
    },
    "Nota": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Nota",
      "type": "object",
      "description": "Represents a single nota (receipt/report) in the NotaKu application.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Nota entity."
        },
        "userId": {
          "type": "string",
          "description": "Reference to User. The ID of the user who created the nota."
        },
        "userEmail": {
          "type": "string",
          "description": "Denormalized email of the user for display purposes."
        },
        "tanggal": {
          "type": "string",
          "description": "Date of the report.",
          "format": "date-time"
        },
        "segmen": {
          "type": "string",
          "description": "The segment/department for the report."
        },
        "noPlatKendaraan": {
          "type": "string",
          "description": "Vehicle license plate number."
        },
        "kmAwal": {
          "type": "number",
          "description": "Starting odometer reading."
        },
        "kmAkhir": {
          "type": "number",
          "description": "Ending odometer reading."
        },
        "namaBarang": {
          "type": "string",
          "description": "Name of the item or service purchased."
        },
        "keterangan": {
          "type": "string",
          "description": "Job description or additional details."
        },
        "nominal": {
          "type": "number",
          "description": "The nominal value/amount of the report."
        },
        "namaPic": {
          "type": "string",
          "description": "Name of the Person in Charge."
        },
        "fotoEvidenUrls": {
          "type": "array",
          "description": "A list of URLs for the evidence photos.",
          "items": {
            "type": "string",
            "format": "uri"
          }
        },
        "dateCreated": {
          "type": "string",
          "description": "Date and time the nota was created.",
          "format": "date-time"
        },
        "status": {
          "type": "string",
          "description": "Verification status of the nota.",
          "enum": [
            "pending",
            "verified"
          ]
        },
        "tanggalVerifikasi": {
            "type": "string",
            "description": "Date the nota was verified by an admin.",
            "format": "date-time"
        }
      },
      "required": [
        "id",
        "userId",
        "userEmail",
        "tanggal",
        "segmen",
        "nominal",
        "namaPic",
        "dateCreated",
        "status"
      ]
    }
  },
  "auth": {
    "providers": [
      "password",
      "anonymous"
    ]
  },
  "firestore": {
    "structure": [
      {
        "path": "/users/{userId}",
        "definition": {
          "entityName": "User",
          "schema": {
            "$ref": "#/backend/entities/User"
          },
          "description": "Stores user profiles. The 'userId' parameter corresponds to the Firebase Auth UID.",
          "params": [
            {
              "name": "userId",
              "description": "The unique identifier for the user, matching the Firebase Auth UID."
            }
          ]
        }
      },
      {
        "path": "/notas/{notaId}",
        "definition": {
          "entityName": "Nota",
          "schema": {
            "$ref": "#/backend/entities/Nota"
          },
          "description": "A global collection of all notas from all users. Access is controlled by security rules based on ownership and roles.",
          "params": [
            {
              "name": "notaId",
              "description": "The unique identifier for the nota."
            }
          ]
        }
      }
    ],
    "reasoning": "The data is structured with a top-level `users` collection for profiles and a top-level `notas` collection for all user-generated notes. This flat structure for `notas` simplifies querying, allowing all users to view all notes easily, which aligns with the application's requirements. Security is enforced within the rules: while reads are permissive, writes (create, update, delete) are strictly controlled. A user can only create a nota with their own `userId`, and can only update or delete notas they own. Admins are granted special privileges to update any nota for verification purposes. This design is efficient and secure for the specified use case."
  }
}
