rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Temporary rule for diagnostics.
      // Allows any read or write to see if the request succeeds at all.
      // WARNING: This is insecure and for testing only.
      allow read, write: if true;
    }
  }
}
